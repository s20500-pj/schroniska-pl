package shelter.backend.rest.model.enums;

public enum AdoptionStatus {
    REQUEST_REVIEW,
    REQUEST_ACCEPETED,
    EMAIL_INVITATION,
    REQUIRES_MANUAL_INVITATION,
    FUTURE_HOME_HOME_VISIT,
    ACCEPTED,
    DECLINED,
    ADOPTED
}
